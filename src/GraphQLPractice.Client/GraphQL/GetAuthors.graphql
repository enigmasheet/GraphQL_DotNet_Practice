query GetAuthors {
  authors(first: 50, order: { name: ASC }) {
    totalCount
    nodes {
      id
      name
      bio
      createdAt
      posts(first: 10) {
        totalCount
        nodes {
          id
          title
          status
        }
      }
    }
  }
}
